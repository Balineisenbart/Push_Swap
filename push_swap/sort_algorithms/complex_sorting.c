#include "push_swap.h"

void complex_sorting(t_node **head_a, t_node **tail_a, t_node **head_b, t_node **tail_b)
{
    int stack_len = stack_len(*head_a);


    while (needs_sorting(*head_a))
    {
        if (find_lis(*head) < stack_length / 3)
            find_sub_lis(*head);
    }
}